<%= form_for(@taker) do |f| %>
  <% if @taker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taker.errors.count, "error") %> prohibited this taker from being saved:</h2>

      <ul>
      <% @taker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :session_id %><br />
    <%= f.text_field :session_id %>
  </div>
  <div class="field">
    <%= f.label :disabled %><br />
    <%= f.check_box :disabled %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
